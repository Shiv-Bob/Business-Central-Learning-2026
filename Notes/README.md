# 📅 Daily Learning Log

A running log of what I'm learning and building in Business Central / AL development.

---

## 2026-06-07
- Set up the repository structure for organized AL learning.
- Created basic examples covering variables, data types, and control structures (`01-AL-Basics`).
- Built a sample table extension on Customer adding a Loyalty Tier enum and Loyalty Points field (`02-Tables-and-Fields`).
- Extended the Customer Card page to surface the new fields and added a quick action to award loyalty points.

## 2026-06-10
- Added event subscriber example reacting to `OnAfterPostSalesDoc` to update loyalty points automatically.
- Created a sample report (`Customer List - Loyalty`) with request page filters.
- Started a custom API page exposing customer loyalty data, plus an HTTP client example for calling external REST APIs.

## 2026-06-12
- Built a small end-to-end sample extension: Customer Tagging (table, list page, management codeunit).
- Verified the project compiles successfully against BC v28 symbols.
- Added `.gitignore` and cleaned up the repo structure.

---

*(Keep adding entries as you go — even short ones. Consistency matters more than length.)*